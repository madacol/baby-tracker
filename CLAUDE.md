# Baby Tracker

Node/SQLite baby tracking app served at `http://localhost:3000`.

## Dev workflow

- Start server: `node --no-warnings server.js`
- Screenshots: save to `.tmp/` inside the workspace, **not** `/tmp`. Delete the folder when done.
